= simple_form_for(@job) do |f|
	= f.input :title, label: "Job Title"
	= f.input :desctiption, label: "Job Description"
	= f.input :company, label: "Your Company"
	= f.input :url, label: "Link to a Job"
	%br/
	= f.button :submit